# Custom Chai Matchers Examples

This is a set of examples of how to create custom Chai matchers.

Install dependencies using `npm` or `yarn`. Run tests using the package script
(`yarn test` or `npm test`).
